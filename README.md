# javascript-calculator-precourse

## 기능

    1. 문자열 입력을 요구하는 기능
    2. 유효한(기본, 커스텀) 구분자를 기준으로 숫자를 추출하고 더하는 기능
        쉼표(,)와 콜론(:)은 기본 구분자로, //와 \n 사이에 위치하는 문자는 커스텀 구분자로 지정
    3. 커스텀 구분자를 설정하는 기능
    4. 예외 처리(숫자 이외의 문자, 유효하지 않은 구분자 사용, 소수점 사용 등)